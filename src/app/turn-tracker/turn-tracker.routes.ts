import { Routes } from '@angular/router';
import { TurnTrackerComponent } from './turn-tracker.component';

export const TURN_TRACKER_ROUTES: Routes = [
  {
    path: '',
    component: TurnTrackerComponent,
  },
];
